# Ex Ninja

Fruit-ninja style slicing game where you upload the things you want to slice.

- `public/index.html` — the whole game (canvas + WebAudio, zero dependencies)
- Served as a Cloudflare Workers static-assets app

## Run locally
```
npx wrangler dev
```

## Deploy
```
npx wrangler deploy
```
